package com.rockdot.project.command {
	import com.rockdot.core.mvc.RockdotEvent;

	public class @command_firstcap@Command extends AbstractCommand {
		override public function execute(event : RockdotEvent = null) : * {
			super.execute(event);

			return null;
		}
	}
}